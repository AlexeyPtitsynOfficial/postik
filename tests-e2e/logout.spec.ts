import {test,expect} from '@playwright/test'

test("log out test", async ({page}) => {

    //await page.getByText('MenuItem').click();

    //expect(page.getByText('MenuItem'), 'Sign in').toBeVisible();
})